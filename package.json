{
  "name": "ci-sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "rimraf reports && jest --config ./jest.json",
    "build": "rimraf dist && tsc && cpy package*.json dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@types/node": "^14.14.0",
    "cpy-cli": "^3.1.1",
    "jest": "^26.6.0",
    "jest-junit": "^12.0.0",
    "prettier": "^2.1.2",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
